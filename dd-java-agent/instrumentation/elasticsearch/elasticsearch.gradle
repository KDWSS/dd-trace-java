apply from: "$rootDir/gradle/java.gradle"

dependencies {
  compileOnly group: 'org.elasticsearch.client', name: 'rest', version: '5.0.0'
  compileOnly group: 'org.elasticsearch', name: 'elasticsearch', version: '6.0.0'
}
